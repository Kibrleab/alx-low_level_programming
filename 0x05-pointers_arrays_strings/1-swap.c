#include "main.h"
/**
 * swap_int - unction that swaps the values of two integers.
 * @a: the firest num
 * @b: the second num
 * @z: integer used to swap
 */
void swap_int(int *a, int *b)
{
	a = 98;
	b = 42;
	_putchar(a '\t');
	_putchar(b '\n');
	int z = a;
	a = b;
	b = z;
	_putchar(a '\t');
        _putchar(b '\n');
}
